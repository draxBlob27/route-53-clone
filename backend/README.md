# Backend

Run locally with `python -m venv .venv && PIP_CACHE_DIR=.pip-cache .venv/bin/pip install -r requirements.txt && .venv/bin/uvicorn main:app --reload --port 8000`.

The SQLite database is created as `route53.db` alongside `main.py`.
